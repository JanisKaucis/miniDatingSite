<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
<link href="build/tailwind.css" rel="stylesheet">
<link href="css/tailwind.css" rel="stylesheet">
<link href="css/style.css" rel="stylesheet">
</head>
<body class="bg-gray-500">
    <button class="bg-pink-500 text-white font-bold rounded-lg border shadow-lg p-2.5" type="button" onclick="location.href = 'logout'">Logout</button>
    <button class="bg-pink-500 text-white font-bold rounded-lg border shadow-lg p-2.5" type="button" onclick="location.href = 'showLiked'">Show Liked</button>
    <div class="centered" >
        {% if gender == 'male' %}
        <div class="text-blue-500">
            {% else %}
            <div class="text-pink-500">
                {% endif %}
            <p>Name: {{ name}}</p>
            <p>Surname: {{ surname}}</p>
            <p>Age: {{ age }}</p>
        </div>

<img src="{{ picture }}" width="500" alt="Image"><br>
        <form method="post" action="/findPeople">
        <input class="bg-pink-500 text-white font-bold rounded-lg border shadow-lg p-2.5" type="submit" name="findPeople" value="Find Opposite Sex">
        </form>
    </div>
    </div>
</body>
</html>